<% if @current_role == 1 || @current_role == 2 || @current_role == 3 %>
    <div class="title">Reservas del Restaurante</div>
    <div class ="muestra-tabla">
            <table class="tabla2">
                <thead>
                    <tr class="tabla2">
                        <th class="tabla2" >
                            <div class="cssToolTip">
                                <%= "Nombre reserva" %>
                                <span>Nombre al que está hecha la Reserva, el color Amarillo significa que el dia de la reserva es del día de hoy en adelante y en rojo que es una Reserva ya Realizada y Finalizada</span> 
                            </div>
                        </th>
                        <th class="tabla2" >
                            <div class="cssToolTip">
                                <%= "Número de personas" %>
                                <span>Número de Personas de las que se compone la REserva</span>
                            </div>
                        </th>
                        <th class="tabla2" title="">
                            <div class="cssToolTip">
                                <%= "Menu" %>
                                <span>Menú del cual se compone la Reserva en caso de tenerlo</span>
                            </div>
                        </th>
                        <th class="tabla2" >
                            <div class="cssToolTip">
                                <%= "Dia reserva" %>
                                <span>Día para el que se realiza la Reserva</span>
                            </div>
                        </th>
                        <th class="tabla2" >
                            <div class="cssToolTip">
                                <%= "Hora reserva" %>
                                <span>Hora para el día de la Reserva</span>
                            </div>
                        </th>
                        <th class="tabla2" >
                            <div class="cssToolTip">
                                <%= "Restaurante" %>
                                <span>Restaurante donde se realiza la Reserva</span>
                            </div>
                        </th>
                        <th class="tabla2" >
                            <div class="cssToolTip">
                                <%= "Teléfono contacto" %>
                                <span>Telefono de contacto opcional del Usuario que realiza la Reserva</span>
                            </div>
                        </th>
                        <th class="tabla2" >
                            <div class="cssToolTip">
                                <%= "Reserva realizada por:" %>
                                <span>Nombre del Usuario que realizó la Reserva</span>
                            </div>
                        </th>
                        <th class="tabla2" >
                            <div class="cssToolTip">
                                <%= "Rol" %>
                                <span>Rol del Usuario que realizó la Reserva</span>
                            </div>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <% @reservations.each do |reservation| %>
                        <tr class="tabla2">
                            <% if reservation.date >= Time.now.beginning_of_day %>
                                <td class="b5"><%= reservation.name %></td>
                            <% else %>
                                <td class="b6"><%= reservation.name %></td>
                            <% end %>
                            <td class="tabla2"><%= reservation.num_persons %></td>
                            <td class="tabla2"><%= reservation.menu.name if !reservation.menu.blank? %></td>
                            <% if reservation.date >= Time.now.beginning_of_day %>
                                <td class="b5"><%= reservation.date.strftime("%d %B %Y") %></td>
                            <% else %>
                                <td class="b6"><%= reservation.date.strftime("%d %B %Y") %></td>
                            <% end %>
                            <td class="tabla2"><%= reservation.hour.strftime("%X") %></td>
                            <td class="tabla2"><%= reservation.restaurant.name %></td>
                            <td class="tabla2"><%= reservation.phone %></td>
                            <td class="tabla2"><%= reservation.user.user_name %></td>
                            <td class="tabla2"><%= RestaurantUser::ROLES[reservation.user.restaurant_users.find_by_restaurant_id(reservation.restaurant.id).rol] %></td>
                        </tr>
                    <% end %>
                </tbody>
                <tfoot>
                    <tr class="tabla2">
                        <th class="tabla2" colspan="9">
                            <%#= will_paginate @reservations %>
                        </th>
                    </tr>
                </tfoot>
            </table>
    </div>
    <div class="muestra-opciones">
            </br><div class="accion">Realizar Reserva</div>
            <%= render 'form' %>
    </div>
<%else%>
        </br><div class="title">Realizar Reserva</div>
        <%= render 'form' %>
<% end %>